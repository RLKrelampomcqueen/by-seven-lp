/**
 * Efeito Uiverse.io (Wendell47) adaptado ao branding by Seven × Trend:
 * blob de gradiente vermelho→laranja girando com blur atrás do conteúdo.
 * Renderizar dentro de qualquer caixa com position:relative (pain-card,
 * benefit, testimonial, badge-card, form-wrap).
 */
export function HoverEffect() {
  return (
    <div className="hoverEffect" aria-hidden="true">
      <div />
    </div>
  );
}
