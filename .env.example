# Endpoint que recebe o lead do formulário em JSON via POST
# (Zapier, n8n, Make, RD Station, HubSpot Forms API, ou endpoint próprio).
# Payload: { nome, email, empresa, cargo, tamanho, dia, horario, consentimento_lgpd, origem }
# Sem essa variável, o formulário mostra a confirmação mas NÃO envia os dados.
VITE_LEAD_WEBHOOK_URL=
